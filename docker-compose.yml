services:
  lemail:
    build: .
    image: lemail:local
    container_name: lemail
    restart: unless-stopped
    environment:
      CONFIG_PATH: /app/config/config.json
    ports:
      - "3000:3000"
      - "25:2525"
    volumes:
      - ./config:/app/config


